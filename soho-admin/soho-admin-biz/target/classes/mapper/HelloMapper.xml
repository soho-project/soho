<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="work.soho.admin.mapper.HelloMapper">

    <sql id="Hello">
        id,
        name,
        value
    </sql>


    <insert id="insertSelective" parameterType="work.soho.api.admin.po.Hello">
        insert into hello
        <trim prefix="(" suffix=")" suffixOverrides=",">

            <if test="id != null">
                id,
            </if>
            <if test="name != null">
                name,
            </if>
            <if test="value != null">
                value,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">

            <if test="id != null">
                #{id,jdbcType=VARCHAR},
            </if>

            <if test="name != null">
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="value != null">
                #{value,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>
    <!--查询投诉表信息-->
    <select id="getById" parameterType="work.soho.api.admin.po.Hello" resultType="work.soho.api.admin.po.Hello">
        SELECT
        id,
        name,
        value
            from hello
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
        </where>
    </select>

</mapper>
